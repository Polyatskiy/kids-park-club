# 🔧 Устранение проблем с пакетной загрузкой

## Ошибка: "Body exceeded 1 MB limit"

### Решение:

В `next.config.mjs` добавлена настройка для увеличения лимита размера тела запроса:

```javascript
experimental: {
  serverActions: {
    bodySizeLimit: '50mb', // До 50MB для пакетной загрузки
  },
},
```

### Важно:

1. **Перезапустите dev-сервер** после изменения `next.config.mjs`:
   ```bash
   # Остановите сервер (Ctrl+C)
   npm run dev
   ```

2. **Если видите предупреждение** `Unrecognized key(s) in object: 'serverActions'`:
   - Это может быть ложное предупреждение в некоторых версиях Next.js
   - Функциональность должна работать несмотря на предупреждение
   - Если не работает, попробуйте убрать `experimental` и оставить только `serverActions`

3. **Проверьте размер файлов**:
   - Максимальный размер одного файла: зависит от настроек Supabase
   - Максимальный размер всех файлов вместе: 50MB (настроено в конфиге)
   - Если нужно больше, увеличьте `bodySizeLimit` до `'100mb'` или больше

## Альтернативное решение (если предупреждение мешает):

Если предупреждение не исчезает, попробуйте использовать API Route вместо Server Action для больших загрузок. Но текущее решение должно работать.

## Проверка работы:

1. Перезапустите сервер
2. Попробуйте загрузить файлы снова
3. Проверьте консоль браузера на наличие ошибок
4. Если ошибка сохраняется, проверьте размер загружаемых файлов

---

**После перезапуска сервера пакетная загрузка должна работать!** 🚀
