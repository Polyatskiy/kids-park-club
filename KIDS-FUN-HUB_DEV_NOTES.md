Этот файл содержит детальное, структурированное описание всего проекта Kids Fun Hub: архитектура, файловая структура, логика работы, разработанные функции, проблемы, которые мы решали, и текущее состояние всех модулей.

Документ предназначен как "мозг проекта", чтобы любой искусственный интеллект или разработчик мог быстро понять, что именно мы построили, где что лежит, как работает и что ещё планируется.

📌 1. Общая концепция проекта

Kids Fun Hub — это детская платформа, построенная на Next.js, с несколькими разделами:

🎨 Разукрашки (SVG + PNG/JPG)

🎧 Аудиосказки

📚 Книги для чтения

🎮 Мини‑игры

🔐 Админ‑панель для загрузки контента

Проект создаётся как коммерческая платформа, с упором на скорость, мобильную адаптацию и расширяемость.

Почти весь контент хранится в Supabase — как в базе данных, так и в Storage.

Главная особенность проекта — свой собственный онлайн‑редактор раскрасок, где можно:

рисовать кистью,

использовать ластик,

заливать области,

менять толщину кисти,

менять прозрачность,

выбирать яркую палитру из десятков цветов,

масштабировать (zoom) изображение,

скачивать результат,

делать undo,

переключать интерфейс панели (справа / снизу),

скрывать/показывать панель.

📌 2. Технический стек
Frontend

Next.js 14+ (App Router)

React 18 (RSC + client components)

TypeScript

Tailwind CSS

framer-motion (легкие анимации)

Backend/Storage

Supabase Database (таблицы контента)

Supabase Storage (SVG, PNG, аудио, обложки книг)

Возможность подключения S3/R2

Хостинг

Vercel (планируется деплой)

📌 3. Структура проекта (папки и файлы)
kids-platform/
│
├── app/
│   ├── page.tsx                 — главная страница
│   ├── coloring/                — список разукрашек
│   │   ├── page.tsx             — каталог
│   │   └── [slug]/page.tsx      — страница конкретной раскраски
│   ├── audio-stories/           — каталог аудиосказок
│   ├── books/                   — каталог книг
│   ├── games/                   — мини-игры (пример 2 игр)
│   └── admin/                   — админ-панель
│
├── components/
│   ├── coloring-canvas.tsx      — ОГРОМНЫЙ компонент редактора
│   ├── ui/                      — кнопки, модалки, панели
│   └── cards/                   — карточки каталога
│
├── lib/
│   ├── supabase.ts              — клиент Supabase (server + client)
│   └── utils.ts                 — вспомогательные функции
│
├── services/
│   └── supabase/                — функции для запросов в бд
│
├── public/
│   └── icons/                   — пользовательские SVG‑иконки (ведро, кисть, ластик...)
│
└── styles/
    └── globals.css              — стили Tailwind
📌 4. Supabase: База данных и Storage
Таблицы:
1. coloring — разукрашки
id
slug
title
category
subcategory
file_path
thumb_url
created_at
type ("svg" или "bitmap")
2. audio_stories — аудиосказки
id
title
slug
file_path
cover_url
description
created_at
3. books — книги
id
title
slug
cover_url
pages (json)
category
created_at
4. games (пока демо)
Storage Buckets:

coloring/ — SVG + PNG/JPG

audio/ — mp3

books/ — картинки страниц

📌 5. Детали проекта по разделам
🎨 Разукрашки
Режим SVG:

заливка по клику (определение области по пикселям);

SVG очищается и заливается корректно.

Режим PNG/JPG:

Сложный "bitmap painter":

двойной canvas: base + overlay;

brush system с динамической прозрачностью;

ластик на destination-out;

заливка реализована вручную через flood fill;

undo хранится в массиве ImageData;

скачивание итогового изображения: объединение 2 canvas в один;

масштабирование через ползунок (70–300%);

скролл естественный (overflow-auto), картинка НЕ растягивается.

Панель инструментов:

выбор цвета (большая палитра, яркие оттенки);

выбор инструмента: заливка / кисть / ластик;

регулировка кисти;

регулировка прозрачности;

undo;

clear (с красивым кастомным confirm);

download;

zoom +/− (ползунок);

кнопка сворачивания панели;

два режима расположения панели: справа (desktop) и снизу (mobile);

иконки — пользовательские SVG, заменённые вручную.

Что было сломано и как чинили:

Палитра ломалась после переноса — исправили индексацию цветов.

Canvas растягивался — переписали алгоритм расчёта размеров.

Зум ломал скролл — убрали onWheel preventDefault.

Плохая прозрачность — изменили формулы и shadowBlur.

Нельзя было рисовать одним кликом — добавили "одиночный dot-stroke".

Панель перекрывала картинку — сделано auto layout.

🎧 Аудиосказки

Загружаются в Storage → запись добавляется в таблицу.

На сайте отображаются карточками.

Воспроизведение через HTML5 audio.

Работаем с Supabase через server components.

Были проблемы с тем, что новые записи не появлялись → решено:

неправильный supabase key,

закешированные роуты,

сброшен revalidate.

📚 Книги

Каждая книга — массив страниц (pages: JSON array).

Обложка отображается в каталоге.

Детальная страница — листалка страниц.

Проблема: записи появлялись в Supabase, но не отображались на сайте. Решение: исправлен Supabase client и revalidation.

🎮 Мини‑игры

Пример: игра реакций и игра "лови предметы".

Простая логика, чтобы показать раздел.

📌 6. Админ‑панель

Добавление разукрашек

Добавление аудиосказок

Добавление книг

Автоматическое создание slug

Автоматическая загрузка в Storage

Автоматическое заполнение таблиц

Планируется: пакетная загрузка через CSV (мы уже частично сделали)

Что ломалось:

добавлялись только разукрашки → выяснено: ошибка в server fetch;

не обновлялось отображение → исрпавили кэш;

неправильное формирование file_path при загрузке.

📌 7. Кэширование Next.js и причины "призраков"

Было несколько ситуаций, когда:

файл удалён из Supabase;

запись удалена;

но карточка всё равно отображалась.

Причины:

ISR закеширован на 60 секунд.

Vercel держит HTML-кэш.

Браузер кэшировал API.

Решение:

export const dynamic = "force-dynamic";
export const revalidate = 0;
📌 8. Логика выбора режима панели

Desktop (ширина > 768px): панель справа.

Mobile: панель снизу.

Панель можно свернуть — остаются только маленькие иконки инструментов.

Панель становится резиновой, чтобы не перекрывать картинку.

📌 9. Что ещё планируем

Пакетная загрузка контента для книг/разыкрашек/аудио.

Автоматическая проверка "битых" записей из Supabase.

Локальное сохранение прогресса раскраски (LocalStorage).

Восстановление раскраски после перезагрузки страницы.

Возможность сохранения в профиль.

Расширенная админка: массовое редактирование.

Категории + подкатегории + теги.

📌 10. Главное о проекте в одном абзаце

Kids Fun Hub — это полностью кастомная детская платформа на Next.js + Supabase с продвинутым редактором раскрасок (SVG/PNG), каталогами аудиосказок, книг, мини-игр и собственной админкой. Весь контент хранится в Supabase, страницы динамические, кэш отключён, редактор работает на двойном canvas со слоями, инструментами, zoom, undo и кастомной панелью управления.
